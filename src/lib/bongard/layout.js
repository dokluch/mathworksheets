/** Geometry of one problem figure, in the units render.jsx and the CSS share. */
export const PANEL = 100
export const GAP = 8
export const DIVIDER = 34
/** Two columns of panels a side, a divider between the sides. */
export const PROBLEM_W = 4 * PANEL + 2 * GAP + DIVIDER
export const PROBLEM_H = 3 * PANEL + 2 * GAP
